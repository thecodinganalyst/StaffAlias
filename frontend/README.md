# Frontend

React/TypeScript frontend for StaffAlias.

The frontend implementation is tracked in issue #5. Environment-specific API endpoints must be externalised, and tenant context must not be hard-coded into production code.
