# Backend

Spring Boot backend for StaffAlias.

The backend implementation is tracked in issue #2. It will support separate `dev` and `prod` profiles and must remain tenant-aware from the beginning.

Domain modules are expected to evolve around areas such as tenant, people, employment, organisation, lifecycle, configuration, and audit.
