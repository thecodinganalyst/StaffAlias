spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/staffalias}
    username: ${DB_USERNAME:staffalias}
    password: ${DB_PASSWORD:staffalias}
  flyway:
    enabled: true

logging:
  level:
    com.thecodinganalyst.staffalias: DEBUG
